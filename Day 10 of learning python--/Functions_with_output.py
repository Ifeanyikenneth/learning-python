# def format_name(f_name, l_name):
#   print(f_name.title())
#   print(l_name.title())


# format_name("kEnnETH", "angela")

# The diffrence between the two code is if ater running this first code you wil see the that the code printed one after the other

def format_name(f_name, l_name):
  formated_f_name = f_name.title()
  formated_l_name = l_name.title()
  return f"{formated_f_name} {formated_l_name}"

print(format_name("kEnnETH", "angela"))
  

def my_function():
  return 3 * 2

print(my_function())
# but now i both capture the two .title() name in a variable 
# using the .title.() allows the str you inputed to all start with upper case
# this are type of function that allow you to have a output once the functiion is completed
# using the return ouput keyword :
#  using it to replace line of code where the function is called, mainly in a function
# at first i added to input to my function one called f_name and the secound called l_name




